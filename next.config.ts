import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  // Enable if you want to use external images
  // images: {
  //   domains: ['github.com', 'avatars.githubusercontent.com'],
  // },
}

export default nextConfig
